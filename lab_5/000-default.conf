# Adjust ports and domain name as per your set up #
<VirtualHost *:8080>
        ServerName lab
        ServerAdmin michal
        DocumentRoot /var/www/html
</VirtualHost>
# Adjust ports and domain name as per your set up #